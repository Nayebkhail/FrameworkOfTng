package Come.pages;

import javax.xml.stream.events.ProcessingInstruction;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.PageFactory;
/*
public class SignIn {
	WebDriver driver;
	
	                
	@FindBy(name = "email") WebElement Sign;
	
	@FindBy(id = "continueOption") WebElement Continue;
	
	
	public SignIn(WebDriver driver2) {
		
		this.driver=driver;
		PageFactory.initElements(driver, this);
		
	}
	public void EnterEmail(String email) {
        //Actions os=new Actions(driver);
	    Sign.sendKeys(email);
		//os.moveToElement(Sign).click().build().perform();
		//os.sendKeys("Naye@gmail.com").build().perform();
		
	
		
	}
	public void ClickContinue() {
		Actions ob=new Actions(driver);
		ob.click(Continue).build().perform();
	}
	}
*/